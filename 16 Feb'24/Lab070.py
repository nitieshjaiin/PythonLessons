# string is a also immutable
name = "Nitish"
name[0] = "A"
print(name) # will throw an error

# string is a bunch of characters
# tuple is list of any data type items which can't changed or mutated
# list is collection of items which can be duplicate and mutated.

